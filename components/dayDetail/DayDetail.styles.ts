import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  itemList: {
    flexWrap: "wrap",
    flexDirection: "row"
  },
  item: {
    width: "50%",
    height: 75
  },
  itemLayout: {
    flexDirection: "row"
  }
});